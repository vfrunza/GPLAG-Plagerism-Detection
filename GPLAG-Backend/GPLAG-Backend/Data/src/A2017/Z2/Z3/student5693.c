#include <stdio.h>

int main() {
    int A[100][100],B[100][100], i=0,j=0,n=0,x1,y1,x2,y2,x3,y3;
    printf("Unesite sirinu i visinu matrice A: ");
    scanf("%d", &A[i][j]);
    printf("Unesite clanove matrice A: ");
    scanf("%d", &A[i]);
    do {
        for(i=0;i<n;i++){
            for(j=0;j<n;j++){
                if((x1,y1)==(x3,y1)){
                     if((x2,y1)==(x3,y2)){
                        if(x3,y1==x3,y3){
                        printf("Da\n");
                        }
                    }
                } 
                else if((x1,y1)==(x3,y1) || (x2,y1)==(x3,y2) || (x3,y1==x3,y3))
        printf("Ne");
            }
        
        }
    }while (i!=j);
	return 0;
}
