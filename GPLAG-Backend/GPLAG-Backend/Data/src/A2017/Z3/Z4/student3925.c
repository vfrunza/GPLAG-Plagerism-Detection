#include <stdio.h>

int main() {
	int k,i=1;
	double suma=0, limit;
	
	printf("Unesite k: ");
	scanf("%d", &k);
	limit=pow(10,-k);
	do{
		clan=(double) predznak/faktorijel(i);
		i++;
	}while(clan>limit);
	return 0;
}
