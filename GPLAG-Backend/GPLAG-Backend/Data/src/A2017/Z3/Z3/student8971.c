#include <stdio.h>


int izbaci_cifre(int broj, int *niz, int vel)
{
	int broj_c[10], i,j,k,cifra,br_c=0, znak=0, pravilnost=1;
	
	for(i=0;i<vel;i++)
	{
		if((*(niz+1)<0) || *(niz+i)>9))
		pravilnost=0;
	}
	if(pravilnost==1)
	{
		if(broj<0)
	}
}
