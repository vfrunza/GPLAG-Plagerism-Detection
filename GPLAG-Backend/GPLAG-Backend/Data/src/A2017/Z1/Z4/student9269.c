#include <stdio.h>
int main() {
	int i, j, r, k, sk;
	printf("Unesite broj redova:");
	printf("Unesite broj kolona:");
	printf("Unesite sirinu jedne kolone:");
	scanf("%d %d %d", &r, &k, &sk)
	for(i=0, i<k, i++)
	{ 
		for(j=0, j<k, j++)
		{
			
		}
	return 0;
}
