#include <stdio.h>

int main() {
	int x,y,z;
	
	do { 
		
	printf ("Unesite broj redova: ");
	scanf (%d,&x);
	printf ("Unesite broj kolona: ");
	scanf (%d,&y);
	printf ("Unesite sirinu jedne kolone: ");
	scanf (%d,&z);
	
	while (x>0 && y>0 && z>0 && x<=10 && y<=10 && z<=10);
	
	
	
	return 0;
}
