#include <stdio.h>

char* zamjena_rijeci (char* tekst, char** rijeci, char** zamjene, int broj_rijeci) {
	char* vrati = tekst;
	
	if (*tekst=='\0' || rijeci == NULL || zamjene == NULL) return vrati; 
	
	return vrati;
}

int main() {
	return 0;
}
