/*B 2017/2018, Zadaća 1, Zadatak 1
	
	NAPOMENA: ulaz/izlaz za zadatke je specificiran
	javnim autotestovima. Zalbe za ne analiziranje testova
	se ne uvazavaju!
	
	NAPOMENA: nece svi (javni) testovi sa zamgera biti 
	dostupni na c9.

	Za sva ostala pitanja i nedoumice vezane za 
	autotestove javiti se na email: bcocalic1@etf.unsa.ba.	
*/
#include <iostream>
#include<vector>

   std :: vector<int>prosti IzdvojiSimetricneTernarne(std :: vector<int>vektor,bool tacno){
    	
    }

int main ()
{
	std :: vector<int> vektor;
	std :: cout<<"Unesite elemente vektora (-1 za kraj): ";
	for(int i=0;i<vektor.size();i++){
		std :: cin>>a.at(i);
	}
	int broj;
	std :: cout<<"Unesite 1 - za proste brojeve, 0 - za slozene brojeve: ";
	std :: cin>>broj;
	std :: vector<int>prosti_brojevi(IzdvojiSimetricneTernarne(vektor,1));
	std :: cout<<"Prosti simetricni brojevi iz vektora su: "<<prosti_brojevi;
	
	return 0;
}