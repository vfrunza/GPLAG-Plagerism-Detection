
#include <iostream>
/*
#include <vector>
#include <deque>

  enum Smjer{Rastuci, Opadajuci};

 std::deque<std::vector<int>> MaksimalniPodnizoviStepenaDvojke (std::vector<int> v, Smjer s){
 	std::vector<int> vs;
 	for(int i=0; i<v.size(); i++){
 		int clan=v.at(i);
 		int c2;
 		while(c2!=0){
 			if(clan%2==0){
 				c2=clan/2;
 				vs.push_back(clan);
 			}
 		}
 		std::vector<int> vs_rastuci, vs_opadajuci;
 		for(int j=0; j<vs.size(); j++){
 		    bool Rastuci(true);
 			if(vs.at(j)<vs.at(j+1)){
 				vs_rastuci.push_back(vs.at(j));
 			}
 			else vs_opadajuci.push_back(vs.at(j));
 		} 
 		if(Rastuci) return vs_rastuci;
 		else return vs_opadajuci;
 	
 }

int main (){
     int n;
    std::vector<int> v;
    std::cout << "Koliko zelite elemenata: ";
    std:: cin >> n;
    std::cout << "Unesite elemente: ";
    int k;
    for(int i=0; i<n; i++){
        std::cin>> k;
        v.push_back(k);
    }
   if(MaksimalniPodnizoviStepenaDvojke(v, true)){
       
   }
  */ int main (){
	return 0;
}