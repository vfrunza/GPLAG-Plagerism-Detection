#include <iostream>
#include <string>
#include <vector>

std::cout<std::string> NapraviPalinrdom(std::<string> recenica, std::vector<std::string> v) {
    for(int i=0; i<int(recenica.size()) {
        
    }
}

int main() {
    
}