/*B 2017/2018, Zadaća 2, Zadatak 2
	
	NAPOMENA: ulaz/izlaz za zadatke je specificiran
	javnim autotestovima. Zalbe za ne analiziranje testova
	se ne uvazavaju!
	
	NAPOMENA: nece svi (javni) testovi sa zamgera biti 
	dostupni na c9.

	Za sva ostala pitanja i nedoumice vezane za 
	autotestove javiti se na forum (c2).
*/
#include <iostream>
#include <vector>
#include <deque>
#include <string>
#include <stdexcept>

enum class SmjerKretanja { NaprijedNazad, NazadNaprijed, GoreDolje, DoljeGore, LijevoDesno, DesnoLijevo };

/*template <typename Tip, typename TipElemenata>
	auto PjescaniSat(Tip<TipElemenata> kont, SmjerKretanja smjer) -> decltype(kont[0]){
		if(kont.size()!=kont.at(0).size() || kont.size()!=kont.at(0).at(0) || kont.at(0).size()!=kont.at(0).at(0).size()) throw std::domain::error("3D matrica nema oblik kocke");
		if(kont.size()%2!=1 || kont.at(0).size()%2!=1 || kont.at(0).at(0).size()%2!=1) throw std::length_error("3D kontejner ne zadovoljava uvjet neparnoti");
	}*/

int main ()
{
	return 0;
}