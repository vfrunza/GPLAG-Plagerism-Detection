/B2016/2017: Zadaća 3, Zadatak 2
#include <iostream>
#include <string>

using namespace std;

map<string, set<int>>KreirajIndeksPojmova(string s) {
    map<string, set<int>> povratni;
    char *p=s;
    while(*p==' ') p++;
    int duzina(0);
    while(*p!=' ' && *p!='\n') {
        auto p=pocetak;
        duzina++;
        p++;
        auto it
        it->first;
    }
}

int main ()
{
	return 0;
}