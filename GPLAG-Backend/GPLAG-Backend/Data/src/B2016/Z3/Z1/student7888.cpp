#include <iostream>
#include <utility>
#include <vector>
#include <functional>
#include <pow>
   std::function<double(double)>BaricentricnaInterpolacija(std::vector<std::pair<double,double>>v,int red) {
        return [v,red](int x) {
                    
                    std::vector<double>v2(v.size(),0);            
            for ( int i = 0 ; i < v.size(); i++ ) {
                  
                  for ( int j = max ; j < min; j++ ) {
                      
                      for ( int j = k)
                      
                  }
                  

                  
                
                
            }
            
            
            
            
        };
        
    }
int main ()
{
	return 0;
}