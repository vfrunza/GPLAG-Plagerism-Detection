#include <iostream>
#include <vector>
#include <deque>
#include <stdexcept>

using std::cin;
using std::cout;

template <typename KontejnerTip, typename SmjerKretanja>
auto IzdvojiDijagonale3D(KontejnerTip kont, SmjerKretanja smjer) -> decltype(kont.at(0) + 0){
    auto prednja(kont.at(0) + 0);
    auto zadnja(kont.at(1) + 0);
    switch(smjer){
        case 0 :
        auto it()
            for(int i = 0; i < prednja.size(); i++){
                
            }
    }
}

int main ()
{
	return 0;
}
