#include <iostream>
#include <vector>
#include <deque>

int main ()
{
    int x,y,z;
    cout << "Unesite dimenzije (x,y,z): ";
    cin >> x >> y >> z;
    vector<deque<deque<int>(x)>(y)>(z); 
	return 0;
}