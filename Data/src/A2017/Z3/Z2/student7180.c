#include <stdio.h>
#include <math.h>

void zaokruzi1(float *niz, int velicina)
{
		float *p = niz;
		while (p < niz+velicina) {
		*p = round((*p)*10)/10;
		p++;
		}

	}

	double preslozi(double *niz, double velicina, double k) {
		int i;
	return 0;
	}
	int main() {
		return 0;
	}
