#include <stdio.h>

int exist(int tr, int niz, int x)
{
    int i;
    for (i=0; i<x, i++)
    if (niz[i]==tr)
    return 1;
return 0;
}
int dva_od_tri(int niz1[], int niz2[], int niz3[], int n1, int n2, int n3)
{
int i=0, tr2, tr3, br=0;
for (i=0; i<x1, i++)
{
    tr2=postoji(niz1[i], niz2, x2);
    tr3=postoji(niz1[i], niz3, x3);
}
