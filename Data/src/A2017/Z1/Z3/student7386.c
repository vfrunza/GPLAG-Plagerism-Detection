#include <stdio.h>
#include <math.h>
#define eps 0.001


int main() {
	int a,b,c;
	double x1,x2, unos, D;
	printf ("Unesite koeficijente a, b i c: ");
	scanf("%d %d %d", &a, &b, &c);
	
	D=b*b-(4*a*c);
	
		if (D<0){
	
	printf("Rjesenja su imaginarni brojevi.");
return 0;
}
while (unos==x1){
printf("Unesite rjesenje x1: ");

if (unos<x1)
printf("Manje");}
	return 0;
}
